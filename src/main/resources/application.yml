spring:
  datasource:
    url: jdbc:postgresql://ec2-44-195-162-77.compute-1.amazonaws.com/d3iqt2pvqo7u5d
    username: ${dbusername}
    password: ${dbpassword}
    driverClassName: org.postgresql.Driver
  jpa:
    database:
      platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  flyway:
    locations:
      classpath:/db_migrations
server:
  port: ${PORT:8080}

